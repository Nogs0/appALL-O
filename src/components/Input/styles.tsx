import { StyleSheet } from "react-native";

export default StyleSheet.create({
    input: {
        width: '80%',
        borderWidth: 2,
        borderRadius: 10,
        borderColor: '#ffffff',
        margin: 10
    }
})