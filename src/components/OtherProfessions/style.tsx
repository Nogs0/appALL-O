import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 0.15,
        alignItems: 'center',
        marginHorizontal: 25
    },
    row: {
        flexDirection: 'row'
    }
})