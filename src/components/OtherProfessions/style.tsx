import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 0.15,
        alignItems: 'center',
        marginHorizontal: 15,
        marginBottom: 10
    },
    row: {
        flexDirection: 'row'
    }
})