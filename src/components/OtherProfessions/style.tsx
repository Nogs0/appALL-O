import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 0.15,
        alignItems: 'center',
        marginHorizontal: 25,
        marginBottom: 10
    },
    row: {
        flexDirection: 'row'
    }
})